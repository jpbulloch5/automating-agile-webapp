version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8

  pre_build:
    commands:
      - pwd
      - ls -al
      - ls -al ~/.m2
#      - cp src/main/build/settings.xml /root/.m2/settings.xml
  build:
    commands:
      - echo Build started on `date`
      - echo test!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
      - mvn install
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/p1-webapp-0.1.war